import React from 'react';

export const VersionCounter = () => {
  return (
    <div className="fixed top-4 left-4 z-50 bg-secondary/80 backdrop-blur-sm rounded-lg px-3 py-1.5 text-sm font-medium text-primary-foreground">
      v4.0.0
    </div>
  );
};